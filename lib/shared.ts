// Docs are served at the site root (parity with the old Docusaurus
// `routeBasePath: "/"`), so the base route is "/".
export const appName = 'Horus';
export const tagline = 'Next generation workflow manager';
export const docsRoute = '/';

// GitHub repository the docs point at.
export const gitConfig = {
  user: 'temple-compute',
  repo: 'horus-runtime',
  branch: 'main',
};
